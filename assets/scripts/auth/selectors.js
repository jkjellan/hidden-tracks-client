'use static'

const authForm = {
  signIn: $('#sign-in'),
  signUpRedirect: $('#sign-up-redirect'),
  signUp: $('#sign-up'),
  changePw: $('#change-password')
}

const sideDrawer = {
  userSignedIn: $('#user-signed-in')
}

module.exports = {
  authForm,
  sideDrawer
}
